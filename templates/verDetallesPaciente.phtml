<?php
require 'header.phtml';
?>
<?php if ($pacientePorId): ?>
    <div class="bg-white shadow-lg rounded-lg p-6 max-w-xl mx-auto">
        <h1 class="text-3xl font-bold mb-8 text-center text-gray-800">Detalles del Paciente</h1>
        
        <div class="mb-6">
            <p class="text-gray-700"><strong>Nombre:</strong> <?php echo $pacientePorId->nombre; ?></p>
            <p class="text-gray-700"><strong>Apellido:</strong> <?php echo $pacientePorId->apellido; ?></p>
        </div>

        <?php if (!empty($pacientePorId->foto)): ?>
            <div class="mb-6 text-center">
                <img src="<?php echo $pacientePorId->foto; ?>" alt="Foto del Paciente" class="rounded-full shadow-lg w-32 h-32 mx-auto">
            </div>
        <?php else: ?>
            <p class="text-gray-500 italic">No hay foto disponible.</p>
        <?php endif; ?>

        <h2 class="text-2xl font-semibold mt-8 mb-4 text-gray-800">Citas</h2>
        <?php if ($citas && count($citas) > 0): ?>
            <ul class="list-disc list-inside">
                <?php foreach ($citas as $cita): ?>
                    <li class="mb-2">
                        <span class="text-gray-700 font-semibold">Fecha:</span> <?php echo $cita->fecha; ?> - 
                        <span class="text-gray-700 font-semibold">Hora:</span> <?php echo $cita->hora; ?>
                    </li>
                <?php endforeach; ?>
            </ul>
        <?php else: ?>
            <p class="text-gray-500 italic">No hay citas registradas para este paciente.</p>
        <?php endif; ?>
    </div>
<?php else: ?>
    <p class="bg-red-500 text-white p-4 rounded-lg text-center">Paciente no encontrado.</p>
<?php endif; ?>

