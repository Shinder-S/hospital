<?php require 'header.phtml'; ?>

<div class="bg-gray-100 min-h-screen py-12">
    <div class="container mx-auto px-4 max-w-md">
        <h1 class="text-3xl font-bold text-gray-900 mb-8 text-center">Editar Paciente</h1>

        <form action="<?= BASE_URL ?>actualizarPaciente" method="POST" class="bg-white shadow-lg rounded-lg p-6">
            <input type="hidden" name="id" value="<?= $paciente->id ?>">

            <div class="mb-4">
                <label for="nombre" class="block text-gray-700 font-bold mb-2">Nombre:</label>
                <input type="text" name="nombre" value="<?= htmlspecialchars($paciente->nombre) ?>" required
                       class="border border-gray-300 rounded-lg py-2 px-4 w-full focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div class="mb-4">
                <label for="apellido" class="block text-gray-700 font-bold mb-2">Apellido:</label>
                <input type="text" name="apellido" value="<?= htmlspecialchars($paciente->apellido) ?>" required
                       class="border border-gray-300 rounded-lg py-2 px-4 w-full focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <div class="mb-4">
                <label for="foto" class="block text-gray-700 font-bold mb-2">Foto (URL):</label>
                <input type="text" name="foto" value="<?= htmlspecialchars($paciente->foto) ?>"
                       class="border border-gray-300 rounded-lg py-2 px-4 w-full focus:outline-none focus:ring-2 focus:ring-blue-500">
            </div>

            <button type="submit" class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded-full w-full mt-4 transition">
                Actualizar
            </button>
        </form>

    </div>
</div>

<?php require 'footer.phtml'; ?>
